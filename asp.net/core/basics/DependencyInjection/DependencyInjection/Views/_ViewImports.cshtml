@{/*
 * @using ServiceContract // for view injections 
 * View injection: 
 * @inject ICitiesService citiesService; 
 * now, we can get the cities in the view:
 * cities = citiesService.GetCities()
 */
}